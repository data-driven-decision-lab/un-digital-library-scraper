---
phase: 03-pipeline-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/un_data_pipeline/dashboard_data_pipeline.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02

must_haves:
  truths:
    - "generate_topic_votes() produces tags at both Main Category and Subcategory levels, not only Main Category matches"
    - "topic_votes_yearly contains no duplicate (Year, Country, TopicTag) rows after a pipeline run"
    - "Tags with no un_classification match are silently dropped, not surfaced as 'No Tag' sentinel rows"
  artifacts:
    - path: "src/un_data_pipeline/dashboard_data_pipeline.py"
      provides: "Rewritten parse_tags_for_subtag1() and updated generate_topic_votes() with dedup"
      contains: "drop_duplicates"
    - path: "src/un_data_pipeline/dashboard_data_pipeline.py"
      provides: "subcategory_keys set built at module top-level alongside main_category_keys"
      contains: "subcategory_keys"
  key_links:
    - from: "generate_topic_votes()"
      to: "parse_tags_for_subtag1()"
      via: "tags column -> list of matched TopicTag strings at Main Category + Subcategory levels"
      pattern: "parse_tags_for_subtag1"
    - from: "generate_topic_votes()"
      to: "df_final before return"
      via: "drop_duplicates(subset=['Year','Country','TopicTag'])"
      pattern: "drop_duplicates"
---

<objective>
Fix the tag parser inside generate_topic_votes() so all applicable UNBIS tags — both
Main Category and Subcategory levels — are retained instead of the current ~29/268.
Also add explicit deduplication before the output DataFrame is returned.

Purpose: The pre-fix baseline of 29 surviving tags out of 268 means 89% of topic-level
voting records are silently dropped. This is PIPE-01 (critical) and PIPE-02 (dedup).

Output: Two targeted edits to dashboard_data_pipeline.py. After a pipeline re-run,
topic_votes_yearly will contain materially more unique TopicTags with zero duplicate rows.
</objective>

<execution_context>
@C:/Users/Gabriel.Omara/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel.Omara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/un_data_pipeline/dashboard_data_pipeline.py

<!-- Key contracts the executor needs — no codebase exploration required. -->

un_classification structure (src/un_data_pipeline/data_modules/un_classification.py):
The dict is: { main_category_string: { subcategory_string: [specific_item, ...] } }
main_category_keys = set(un_classification.keys()) is already built at module top-level (~line 37).
subcategory keys are the inner dict keys of each main category value.

Current broken parser inside generate_topic_votes() (~lines 586-603):
  - Walks tags left-to-right; only matches against main_category_keys.
  - Returns at most ONE tag per Main Category (first subcategory match or the main cat itself).
  - Returns ["No Tag"] sentinel when nothing matches instead of [].
  The result: only 29 of 268 tags survive; entire categories like DISARMAMENT are dropped.

generate_topic_votes() return block (~lines 640-646):
  final_cols_order = ['Year', 'Country', 'TopicTag', 'YesVotes_Topic',
                      'NoVotes_Topic', 'AbstainVotes_Topic', 'TotalVotes_Topic']
  df_final = df_final[final_cols_order]
  return df_final   # <-- dedup line goes here, before the return

Module-level setup block (~lines 36-43):
  try:
      from src.un_data_pipeline.data_modules.un_classification import un_classification
      main_category_keys = set(un_classification.keys())
      # subcategory_keys must be added immediately after this line
      logging.info(...)
  except ImportError:
      un_classification = None
      main_category_keys = set()
      # subcategory_keys = set() must be added here too
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite parse_tags_for_subtag1() to capture all matching tags</name>
  <files>src/un_data_pipeline/dashboard_data_pipeline.py</files>
  <behavior>
    - "DISARMAMENT, NUCLEAR DISARMAMENT, ARMS EMBARGO" where all three are in main_category_keys or subcategory_keys -> returns all three strings, not just first
    - Tag string with only subcategory-level tags (no parent main cat present) -> subcategory tags still returned
    - None or NaN input -> returns []
    - No un_classification matches in string -> returns [] (empty list, NOT "No Tag")
    - Duplicate tags within one string -> deduplicated in output
    - Multiple Main Categories present -> ALL returned, not just first
  </behavior>
  <action>
Make three targeted edits to dashboard_data_pipeline.py. Do not change any other logic.

EDIT 1: Add subcategory_keys at module top-level.
In the try block that imports un_classification (around line 37), after
`main_category_keys = set(un_classification.keys())`, add:
    subcategory_keys = set()
    for _mc_dict in un_classification.values():
        subcategory_keys.update(_mc_dict.keys())
In the except ImportError fallback, after `main_category_keys = set()`, add:
    subcategory_keys = set()

EDIT 2: Replace parse_tags_for_subtag1() body inside generate_topic_votes().
Replace the entire current function (the while-loop implementation) with:
    def parse_tags_for_subtag1(tag_string):
        if pd.isna(tag_string) or not isinstance(tag_string, str):
            return []
        tag_items = [item.strip() for item in tag_string.split(',') if item.strip()]
        matched = []
        for item in tag_items:
            if item in main_category_keys or item in subcategory_keys:
                matched.append(item)
        return list(dict.fromkeys(matched))  # dedupe preserving insertion order

EDIT 3: Add deduplication in generate_topic_votes() before the return statement.
After `df_final = df_final[final_cols_order]` and before `return df_final`, add:
    df_final.drop_duplicates(subset=['Year', 'Country', 'TopicTag'], inplace=True)
  </action>
  <verify>
    <automated>cd "C:/WS/un-digital-library-scraper" && python -c "
import sys; sys.path.insert(0, '.')
import src.un_data_pipeline.dashboard_data_pipeline as p

assert hasattr(p, 'subcategory_keys'), 'FAIL: subcategory_keys missing from module level'
assert len(p.subcategory_keys) > 50, f'FAIL: subcategory_keys too small: {len(p.subcategory_keys)}'

import pandas as pd
from src.un_data_pipeline.data_modules.un_classification import un_classification
mc_sample = next(iter(p.main_category_keys))
sc_sample = next(iter(un_classification[mc_sample].keys()))

df_test = pd.DataFrame({
    'Year': [2025, 2025],
    'Resolution': ['A/RES/79/1', 'A/RES/79/2'],
    'tags': [mc_sample + ', ' + sc_sample, 'junk tag only'],
    'USA': ['YES', 'NO'],
    'GBR': ['YES', 'YES'],
})
result = p.generate_topic_votes(df_test)

assert not result.empty, 'FAIL: empty result for valid input'
tags_found = set(result['TopicTag'].unique())
assert mc_sample in tags_found or sc_sample in tags_found, f'FAIL: expected tags not found: {tags_found}'
assert 'No Tag' not in tags_found, 'FAIL: No Tag sentinel still present'
dupes = result.duplicated(subset=['Year', 'Country', 'TopicTag'])
assert dupes.sum() == 0, f'FAIL: {dupes.sum()} duplicate rows'

print('PASS: subcategory_keys=' + str(len(p.subcategory_keys)) + ', main_category_keys=' + str(len(p.main_category_keys)))
print('PASS: tags_found=' + str(tags_found))
print('PASS: zero duplicates')
"
    </automated>
  </verify>
  <done>
    parse_tags_for_subtag1() returns all matching tags at both Main Category and Subcategory
    levels. subcategory_keys exists at module top-level. generate_topic_votes() output has
    zero duplicate (Year, Country, TopicTag) rows. The "No Tag" sentinel is gone.
  </done>
</task>

</tasks>

<verification>
Run the automated verify command from Task 1. Expected output:
- "PASS: subcategory_keys=N, main_category_keys=M" where N > 50
- "PASS: tags_found=..." containing the sampled mc/sc tag names
- "PASS: zero duplicates"

Secondary check: grep dashboard_data_pipeline.py for "subcategory_keys" (should appear 3+ times),
"No Tag" (should NOT appear), and "drop_duplicates" (should appear at least once).
</verification>

<success_criteria>
- subcategory_keys is built at module top-level and has >50 entries
- parse_tags_for_subtag1() returns a list of ALL matching tags (Main Category + Subcategory) from a comma-separated tag string, not just the first Main Category match
- The "No Tag" sentinel string is removed from the codebase
- generate_topic_votes() calls drop_duplicates on ['Year', 'Country', 'TopicTag'] before returning
- The automated verify command exits 0
</success_criteria>

<output>
After completion, create .planning/phases/03-pipeline-fixes/03-01-SUMMARY.md
</output>
