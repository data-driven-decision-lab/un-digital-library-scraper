---
phase: 04-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cloudbuild.yaml
  - Dockerfile
  - .env.example
autonomous: true
requirements:
  - CICD-01
  - CICD-02
  - CICD-03

must_haves:
  truths:
    - "cloudbuild.yaml passes TURSO_DATABASE_URL and TURSO_AUTH_TOKEN to the Cloud Run deploy step"
    - "Dockerfile installs libsql-experimental from requirements.txt"
    - ".env.example lists TURSO_DATABASE_URL and TURSO_AUTH_TOKEN placeholder values with comments"
  artifacts:
    - path: "cloudbuild.yaml"
      provides: "CI/CD pipeline config with Turso env vars wired to Cloud Run"
      contains: "TURSO_DATABASE_URL"
    - path: "Dockerfile"
      provides: "Container image that installs libsql-experimental"
      contains: "libsql-experimental"
    - path: ".env.example"
      provides: "Template for required environment variables"
      contains: "TURSO_DATABASE_URL"
  key_links:
    - from: "cloudbuild.yaml"
      to: "Cloud Run service"
      via: "gcloud run deploy --set-env-vars"
      pattern: "TURSO_DATABASE_URL|set-env-vars"
---

<objective>
Update CI/CD configuration and create .env.example for Turso credentials.

Purpose: The pipeline now requires TURSO_DATABASE_URL and TURSO_AUTH_TOKEN at runtime. These must be injected at deploy time (Cloud Run) and documented for local dev.
Output: Updated cloudbuild.yaml, confirmed Dockerfile, .env.example file.
</objective>

<execution_context>
@C:/Users/Gabriel.Omara/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel.Omara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@cloudbuild.yaml
@Dockerfile
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cloudbuild.yaml with Turso env vars in Cloud Run deploy step</name>
  <files>cloudbuild.yaml</files>
  <action>
    Add `--set-env-vars` flag to the existing `gcloud run deploy unreportapi` step so Turso credentials are injected at deploy time.

    The Cloud Run deploy step currently has these args:
    ```
    - 'run'
    - 'deploy'
    - 'unreportapi'
    - '--image'
    - 'gcr.io/$PROJECT_ID/unreportapi:$COMMIT_SHA'
    - '--region'
    - 'europe-west1'
    - '--platform'
    - 'managed'
    - '--allow-unauthenticated'
    ```

    Append these two additional args to the same step:
    ```
    - '--set-env-vars'
    - 'TURSO_DATABASE_URL=$$TURSO_DATABASE_URL,TURSO_AUTH_TOKEN=$$TURSO_AUTH_TOKEN'
    ```

    The `$$` prefix is the Cloud Build substitution escape for secret/env vars — double-dollar expands to a single `$` at runtime. The values `$$TURSO_DATABASE_URL` and `$$TURSO_AUTH_TOKEN` must be available as Cloud Build substitution variables or Secret Manager-injected values. Do NOT hardcode credential values.

    Also remove the stale `--allow-unauthenticated` comment if present; keep the flag itself.
  </action>
  <verify>
    Read cloudbuild.yaml and confirm:
    - `--set-env-vars` arg is present in the deploy step
    - `TURSO_DATABASE_URL=$$TURSO_DATABASE_URL` is in the value string
    - `TURSO_AUTH_TOKEN=$$TURSO_AUTH_TOKEN` is in the value string
    - YAML parses without error: `python -c "import yaml; yaml.safe_load(open('cloudbuild.yaml'))"`
  </verify>
  <done>cloudbuild.yaml deploy step includes --set-env-vars with both Turso variables using $$-escaped references</done>
</task>

<task type="auto">
  <name>Task 2: Verify Dockerfile installs libsql-experimental and create .env.example</name>
  <files>Dockerfile, .env.example</files>
  <action>
    **Dockerfile check:**
    Dockerfile already copies `requirements.txt` and runs `pip install -r requirements.txt`. Since `libsql-experimental>=0.0.5` is already in requirements.txt (added in Phase 2), no Dockerfile changes are needed. Confirm by reading the file — do NOT duplicate the install line. CICD-02 is satisfied by the existing requirements.txt install.

    **Create .env.example:**
    Create `.env.example` at the project root with the following content exactly:

    ```
    # UN Digital Library Scraper — Environment Variables
    # Copy this file to .env and fill in actual values before running locally.
    # NEVER commit .env to version control.

    # -----------------------------------------------------------------------
    # Turso (LibSQL) Database — REQUIRED
    # Obtain from: Turso Dashboard -> Your Database -> Connect -> libsql
    # -----------------------------------------------------------------------
    TURSO_DATABASE_URL=libsql://your-database-name.turso.io
    TURSO_AUTH_TOKEN=your-turso-auth-token-here

    # -----------------------------------------------------------------------
    # OpenAI API — REQUIRED for scraper tagging
    # Obtain from: https://platform.openai.com/api-keys
    # -----------------------------------------------------------------------
    API_KEY=your-openai-api-key-here

    # -----------------------------------------------------------------------
    # Optional
    # -----------------------------------------------------------------------
    LOG_LEVEL=INFO
    PIPELINE_SOURCE_TABLE=un_votes_with_sc
    ```

    The placeholder values (e.g., `libsql://your-database-name.turso.io`) must be clearly fake — not real credentials.
  </action>
  <verify>
    - `.env.example` exists at project root: `test -f .env.example`
    - Contains TURSO_DATABASE_URL: `grep -c "TURSO_DATABASE_URL" .env.example`
    - Contains TURSO_AUTH_TOKEN: `grep -c "TURSO_AUTH_TOKEN" .env.example`
    - Contains API_KEY: `grep -c "API_KEY" .env.example`
    - .env.example is NOT in .gitignore (it should be committed): `grep ".env.example" .gitignore` should return nothing or show it's whitelisted
  </verify>
  <done>.env.example exists with Turso and OpenAI credential placeholders; Dockerfile confirmed to install libsql-experimental via requirements.txt</done>
</task>

</tasks>

<verification>
- `python -c "import yaml; yaml.safe_load(open('cloudbuild.yaml'))"` passes (YAML valid)
- `grep "TURSO_DATABASE_URL" cloudbuild.yaml` returns a match
- `grep "TURSO_AUTH_TOKEN" cloudbuild.yaml` returns a match
- `grep "TURSO_DATABASE_URL" .env.example` returns a match
- `test -f .env.example && echo "exists"` prints "exists"
</verification>

<success_criteria>
1. cloudbuild.yaml deploy step injects TURSO_DATABASE_URL and TURSO_AUTH_TOKEN via --set-env-vars
2. .env.example lists all required credential placeholders with source comments
3. Dockerfile unchanged — libsql-experimental already covered by requirements.txt
</success_criteria>

<output>
After completion, create `.planning/phases/04-ship/04-01-SUMMARY.md`
</output>
