---
phase: 01-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/un_report_apiold/
  - turso_stuff.txt
  - PIPELINE_ISSUES.md
  - RECOMPUTATION_GUIDE.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-04

must_haves:
  truths:
    - "src/un_report_apiold/ no longer exists anywhere in the repository"
    - "turso_stuff.txt (plaintext credentials) is not present in the repository"
    - "Stale analysis markdown files (PIPELINE_ISSUES.md, RECOMPUTATION_GUIDE.md) are removed or archived"
    - "No references to un_report_apiold remain in any tracked file"
  artifacts:
    - path: "src/un_report_api/"
      provides: "Single active API directory — only one API implementation exists"
      contains: "app/main.py"
    - path: ".planning/phases/01-cleanup/01-01-SUMMARY.md"
      provides: "Completion record for this plan"
  key_links:
    - from: "src/un_report_api/app/main.py"
      to: "running application"
      via: "Dockerfile references un_report_api, not un_report_apiold"
      pattern: "un_report_api"
---

<objective>
Remove the dead legacy API directory and clear plaintext secrets and stale analysis artifacts from the repository root.

Purpose: The `src/un_report_apiold/` directory is a full duplicate of the live API with outdated logic. It creates maintenance burden, inflates deployment size, and causes confusion about which implementation is current. The root-level `turso_stuff.txt` file contains a plaintext Turso auth token — a security risk. The root-level `PIPELINE_ISSUES.md` and `RECOMPUTATION_GUIDE.md` are audit/analysis artifacts that belong in the planning folder, not the repository root.

Output: A src/ directory with one API implementation, no plaintext credentials at root, and no stale analysis markdown at root.
</objective>

<execution_context>
@C:/Users/Gabriel.Omara/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gabriel.Omara/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/un_report_apiold/ and remove plaintext credentials file</name>
  <files>src/un_report_apiold/, turso_stuff.txt</files>
  <action>
    Delete the entire `src/un_report_apiold/` directory tree from the repository. This is a complete duplicate of `src/un_report_api/` committed for historical reference only — it is not imported, not mounted in the Dockerfile, and not referenced by any live code. Verify with `grep -rn "un_report_apiold" src/ .github/` before deleting to confirm no live imports.

    Also delete `turso_stuff.txt` from the repository root. This file contains a Turso database URL and auth token in plaintext (copied from a chat message). These credentials belong exclusively in `.env` (already gitignored). Confirm `.env` is in `.gitignore` before deleting.

    Commands:
    ```
    git rm -r src/un_report_apiold/
    git rm turso_stuff.txt
    ```

    Do NOT touch:
    - `src/un_report_api/` (the live API — keep intact)
    - `.env` (local credentials file, already gitignored)
    - Any file inside `src/un_data_pipeline/`
  </action>
  <verify>
    <automated>python -c "import os, sys; fails = []; [fails.append(p) for p in ['src/un_report_apiold', 'turso_stuff.txt'] if os.path.exists(p)]; print('PASS' if not fails else f'FAIL: still exists: {fails}'); sys.exit(0 if not fails else 1)"</automated>
  </verify>
  <done>
    `src/un_report_apiold/` is absent from the filesystem. `turso_stuff.txt` is absent from the filesystem. `git status` shows both as deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move stale analysis docs out of repository root</name>
  <files>PIPELINE_ISSUES.md, RECOMPUTATION_GUIDE.md</files>
  <action>
    Move `PIPELINE_ISSUES.md` and `RECOMPUTATION_GUIDE.md` from the repository root into `.planning/` so they are preserved as planning context but not presented as live project documentation to contributors.

    These files are the audit outputs that informed this cleanup phase. They should be kept for reference during subsequent phases but must not sit at the repository root alongside `README.md`, `Dockerfile`, and `requirements.txt`.

    Commands:
    ```
    git mv PIPELINE_ISSUES.md .planning/PIPELINE_ISSUES.md
    git mv RECOMPUTATION_GUIDE.md .planning/RECOMPUTATION_GUIDE.md
    ```

    Do NOT delete these files — they contain issue analysis referenced by Phase 3 planning.
  </action>
  <verify>
    <automated>python -c "import os, sys; checks = [('PIPELINE_ISSUES.md', False), ('RECOMPUTATION_GUIDE.md', False), ('.planning/PIPELINE_ISSUES.md', True), ('.planning/RECOMPUTATION_GUIDE.md', True)]; fails = [(p, exp) for p, exp in checks if os.path.exists(p) != exp]; print('PASS' if not fails else f'FAIL: {fails}'); sys.exit(0 if not fails else 1)"</automated>
  </verify>
  <done>
    `PIPELINE_ISSUES.md` and `RECOMPUTATION_GUIDE.md` no longer exist at the repository root. Both exist under `.planning/`. `git status` shows two renames.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify no dead references remain:
```bash
grep -rn "un_report_apiold" src/ .github/ Dockerfile README.md 2>/dev/null
```
Expected: no output (zero matches).

Verify Dockerfile still references the live API path:
```bash
grep "un_report_api" Dockerfile
```
Expected: references `un_report_api` (without "old").

Verify git status is clean or shows only expected deletions/renames:
```bash
git status
```
</verification>

<success_criteria>
- `src/un_report_apiold/` does not exist
- `turso_stuff.txt` does not exist at repository root
- `PIPELINE_ISSUES.md` does not exist at repository root
- `RECOMPUTATION_GUIDE.md` does not exist at repository root
- `.planning/PIPELINE_ISSUES.md` and `.planning/RECOMPUTATION_GUIDE.md` exist
- `grep -rn "un_report_apiold" src/ .github/ Dockerfile README.md` returns zero matches
- `src/un_report_api/` still intact with all original files
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup/01-01-SUMMARY.md` using the summary template.
</output>
